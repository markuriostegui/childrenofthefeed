[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-empire-research-os"
version = "0.1.0"
description = "Plain-text-first research operating system for the AI Empire dossier"
requires-python = ">=3.10"

